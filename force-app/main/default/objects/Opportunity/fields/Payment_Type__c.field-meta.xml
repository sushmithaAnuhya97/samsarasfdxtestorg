<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Payment_Type__c</fullName>
    <description>Only applies to revenue opportunities because rebate opportunities are not marked upfront/direct annual/direct monthly.</description>
    <externalId>false</externalId>
    <formula>IF((NOT
(ISBLANK(Reseller__c)
)
), &quot;Reseller&quot;,

IF(NOT
(ISBLANK(Finance_Partner__c)
)
, &quot;Finance Partner&quot;,

IF(
ISPICKVAL(Selected_Payment_Type__c, &quot;Direct Monthly&quot;),
&quot;Samsara Financed&quot;,
IF
(ISPICKVAL(Selected_Payment_Type__c, &quot;Direct Quarterly&quot;),
&quot;Samsara Financed&quot;,
IF
(ISPICKVAL(Selected_Payment_Type__c, &quot;Direct Semi-Annual&quot;),
&quot;Samsara Financed&quot;,
IF
(ISPICKVAL(Selected_Payment_Type__c, &quot;Direct Annual&quot;),
&quot;Samsara Financed&quot;,

IF
(OR
(ISPICKVAL(Selected_Payment_Type__c, &quot;Upfront Payments&quot;),
ISPICKVAL(Selected_Payment_Type__c, &quot;&quot;)),&quot;Upfront&quot;,

NULL)
)
)
)
)
)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>Only applies to revenue opportunities because rebate opportunities are not marked upfront/direct annual/direct monthly.</inlineHelpText>
    <label>Payment Type</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
