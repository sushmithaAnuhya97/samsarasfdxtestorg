<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AtRisk__c</fullName>
    <formula>if(and(Probability &gt;= 0.10, Probability &lt; 1, Express_Flag__c=1),  Amount*3,

  if(and(Probability &gt;= 0.10, Probability &lt; 1, Express_Flag__c=0), Amount, 

     if(and(Probability=1, Express_Flag__c=1), Amount*3, 

        if(and(Probability=1, Express_Flag__c=0), Amount,

        0)
     )
  )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>$AtRisk</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Currency</type>
</CustomField>
