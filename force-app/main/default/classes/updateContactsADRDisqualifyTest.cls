@isTest(SeeAllData=false) 
private class updateContactsADRDisqualifyTest {

    static testMethod void testADRDisquaify() {
    
    /*
        // insert account
        Account a = new Account (Name = 'Acme Co', Organization_Size__c = '5000+', BillingCountry = 'Canada', Industry = 'Other', ADR_Transfer__c = false);
        insert a;
        
        // insert contact
        Contact cntc = new Contact(AccountId = a.Id, FirstName = 'Cntc', LastName = 'Test', ADR_Transfer_Status__c = 'Demo Scheduled', ADR_Transfer_By__c = '0051a0000015njF');            
        insert cntc;
        
        // check if status is demo scheduled
        System.assertEquals('Demo Scheduled', [SELECT  ADR_Transfer_Status__c FROM Contact WHERE FirstName = 'Cntc' LIMIT 1]. ADR_Transfer_Status__c);

        // update status to disquaify and execute trigger
        cntc.ADR_Transfer_Status__c = 'Disqualified - No Show';
        update cntc;
        
        // check if status changed
        System.assertEquals('Disqualified - No Show', [SELECT  ADR_Transfer_Status__c FROM Contact WHERE FirstName = 'Cntc' LIMIT 1]. ADR_Transfer_Status__c);
        
        // create the user and send the email
        User toUser = [Select Email, FirstName From User where Id = '0051a0000015njF'];
       */
        
    
    }
    
    
    }